#include "hello.h"

/**
 * A brief test description of main function.
 * 
 * the desc continued
 * the desc 2nd line
 *
 * @param name The definitin of parameter name
 */
void func(int name) {
}

int main()
{
    func(0);
    int* p = new int[5];
    hello();

    return 0;
}

